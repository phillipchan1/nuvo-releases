# Nuvo releases

Signed, notarized macOS builds of [Nuvo](https://nuvo.day) + the auto-update manifest (`latest.json`). Published automatically by CI from the app repo. The `Nuvo.dmg` asset on the latest release is always the newest build.
